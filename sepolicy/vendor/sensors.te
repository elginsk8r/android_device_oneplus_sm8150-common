# Allow sensors to get vendor_sensors_prop
get_prop(vendor_sensors, vendor_sensors_prop)

# Allow sensors to read project info
allow vendor_sensors sysfs_project_info:dir search;
allow vendor_sensors sysfs_project_info:file r_file_perms;
